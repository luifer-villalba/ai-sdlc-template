# docker-compose.yml

services:
  app:
    build: .
    ports:
      - "8888:8888"
    environment:
      - DEBUG=true
      - APP_RUN_COMMAND=${APP_RUN_COMMAND:-python -m http.server 8888}
    volumes:
      - .:/app
    command: sh -lc "$$APP_RUN_COMMAND"
